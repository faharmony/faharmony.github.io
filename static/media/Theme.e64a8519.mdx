`Theme` object contains the various methods to return required value.

```jsx live=true
import { Theme } from "@fasolutions/harmony";
<div style={{color: Theme.colorText('primary')}}>
```

# Hello, world!

```js
import React from "react";
import Example from "./place";

export default () => <Example />;
```

```js render=true
<div
  style={{
    backgroundColor: "rebeccapurple",
    padding: "20px",
    marginTop: "20px",
  }}
/>
```

```js live=true
<div style={{ backgroundColor: "tomato", padding: "20px" }} />
```
